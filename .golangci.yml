version: "2"

run:
  timeout: 5m
  relative-path-mode: gomod

linters:
  default: standard
  enable:
    - bodyclose
    - contextcheck
    - exhaustive
    - gosec
    - loggercheck
    - modernize
    - nilerr
    - nilnesserr
    - nilnil
    - noctx
    - rowserrcheck
    - sloglint
    - sqlclosecheck
formatters:
  enable:
    - gofumpt
    - goimports

issues:
  max-issues-per-linter: 0
  max-same-issues: 0
